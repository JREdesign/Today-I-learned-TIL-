# JavaScript – Array.from

Array.from permite crear arrays a partir de objetos iterables o parecidos a arrays.

Ejemplo con string:

    Array.from("hola") // ["h", "o", "l", "a"]

Ejemplo con longitud:

    Array.from({ length: 3 }, (_, i) => i) // [0, 1, 2]
